{
  "name": "niksremodeling-website",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs"
  },
  "engines": { "node": ">=20" }
}
